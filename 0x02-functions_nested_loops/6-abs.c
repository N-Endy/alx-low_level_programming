#include "main.h"

/**
 * _abs - Prints the absolute value of an integer
 * @c: Takes in an integer as argument
 * Return: Returns an integer value
 */

int _abs(int c)
{
	return c < 0 ? c * -1 : c;
}
